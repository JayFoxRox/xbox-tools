A tool to load controller configuations to accept input from Xbox input devices.

You might have to manually unload your current kernel driver.
On Linux this is typically done using `sudo rmmod xpad`.
